/**
 * Sprint goal service.
 */
export class SprintGoalService {

    /**
     * Based off the current team ID, get the default area path.
     *
     * @param teamId the team id
     */
    public static async findTeamDefaultAreaPath(teamId: string):Promise<string> {
        return "";
    }
}